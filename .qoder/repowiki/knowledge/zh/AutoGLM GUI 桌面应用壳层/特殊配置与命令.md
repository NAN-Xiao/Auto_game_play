- **构建前校验**：`npm run prebuild` 触发 `verify-deps.js` 确保 `electron-updater` 等依赖存在。
- **权限修复**：`electron-builder` 配置了 `afterPack: ./afterPack.js`，在打包完成后自动为嵌入的 Python 后端及 ADB 工具链设置 `0o755` 执行权限，解决跨平台权限丢失问题。
- **环境区分**：启动时通过 `--dev` 参数区分开发模式（控制台日志、`uv run` 启动）与生产模式（文件日志、直接执行二进制）。