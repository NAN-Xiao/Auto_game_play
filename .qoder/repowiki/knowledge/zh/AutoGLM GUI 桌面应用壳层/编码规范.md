- 主进程统一使用 `electron-log` 替代 `console` 进行持久化日志记录，并通过 `writeMainLog` 封装实现日志级别管理与内容裁剪。
- 采用性能埋点模式，在关键启动阶段（如端口查找、后端启动、窗口创建）调用 `perfMark` 记录时间戳，并在启动完成后输出分段耗时报告。
- 资源路径获取统一通过 `getResourcePath` 工具函数处理，根据 `app.isPackaged` 状态动态切换 `process.resourcesPath` 或项目根目录相对路径。