- **测试框架**：pytest (含 `pytest.ini` 配置) 与 httpx (异步HTTP客户端)。
- **Mock服务**：FastAPI + uvicorn 用于构建轻量级Mock LLM和Device Agent服务器。
- **数据校验**：Pydantic (`schema.py`) 用于测试场景配置的强类型校验。
- **图像处理**：Pillow (PIL) 用于在状态机中处理截图元数据。