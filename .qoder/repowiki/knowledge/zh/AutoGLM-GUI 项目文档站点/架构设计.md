- 采用 Docusaurus 3.9 框架与 Classic 预设，通过 `docusaurus.config.ts` 集中管理站点元数据、路由前缀及主题配置。
- 内容架构由 `sidebars.ts` 定义，将 Markdown 文档按快速开始、功能特性、开发指南等逻辑分类组织。
- 首页入口位于 `src/pages/index.tsx`，集成自定义组件 `HomepageFeatures` 展示核心亮点。
- 启用 Mermaid 插件支持流程图渲染，并通过 `custom.css` 实现全局样式定制。