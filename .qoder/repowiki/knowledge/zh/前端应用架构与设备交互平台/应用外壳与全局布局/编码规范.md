- 通用 UI 组件采用受控或回调驱动的设计，通过 props 暴露状态与事件（如 Toast 的 onClose、ConfirmDialog 的 onConfirm/onCancel）。
- 自定义 Hooks 以 use 前缀命名，封装浏览器 API 或持久化逻辑，并在内部处理边界条件与错误兜底。
- 国际化文本统一通过 i18n 上下文的 useTranslation/useLocale 获取，避免在组件内硬编码文案。
- 主题相关组件使用 next-themes 的 useTheme 钩子，保持与 ThemeProvider 的行为一致并支持系统级切换。