- 采用受控组件模式管理复杂表单状态，如 `DeviceSidebar` 中的 WiFi 配对流程与 `GroupManageDialog` 中的分组编辑。
- 在长列表或高频更新场景下（如设备轮询），利用 `useRef` 配合 `areDevicesEqual` 进行深度比对以优化重渲染性能。
- 统一使用 `showToast` 回调函数处理异步操作反馈，确保跨组件的提示样式与行为一致性。
- 通过 `localStorage` 持久化用户界面偏好，包括侧边栏折叠状态、宽度以及设备分组的展开/收起状态。