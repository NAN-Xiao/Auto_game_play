- **视频解码**：使用 `@yume-chan/scrcpy-decoder-webcodecs` 结合原生 `VideoDecoder` API 实现低延迟 H.264 硬件解码。
- **实时通信**：基于 `socket.io-client` 建立双向 WebSocket 通道，用于传输视频元数据、控制指令及任务事件流。
- **状态管理**：采用 React Context API (`DeviceContext`, `I18nContext`) 配合自定义 Hooks 实现跨组件状态共享与副作用管理。