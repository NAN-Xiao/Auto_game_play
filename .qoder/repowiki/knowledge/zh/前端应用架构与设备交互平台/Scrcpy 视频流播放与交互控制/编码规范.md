- 使用 `useRef` 存储最新的事件回调函数（如 `onFallbackRef`）及状态标志（如 `isVisibleRef`），以避免在 `useEffect` 或异步闭包中产生陈旧闭包问题。
- 在鼠标/触控事件处理中引入节流机制（Throttling），通过 `setTimeout` 限制 `sendTouchMove` 等高频 API 调用的执行频率（默认 50ms）。
- 实现基于页面可见性（Visibility）的连接管理逻辑，当组件不可见时主动断开 Socket 连接并抑制自动重连，以节省后端资源。