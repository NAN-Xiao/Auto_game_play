- 采用单例模式管理全局状态，如 `DeviceManager.get_instance()` 和 `UnifiedConfigManager`。
- 使用 Python `Protocol` 定义核心接口（`AsyncAgent`, `DeviceProtocol`），确保不同实现间的类型兼容与解耦。
- 在异步智能体实现中广泛使用 `AsyncIterator` 进行流式事件输出，支持原生的 `async for` 消费与即时取消。
- 配置管理遵循严格的优先级覆盖逻辑，并通过 `explicit_keys` 追踪各层级配置的显式声明状态。